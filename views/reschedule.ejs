<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    /* Styles for the Profile Icon */
.profile-icon {
  cursor: pointer;
  /* make it smaller */
  width: 50px;
  height: 50px;
  /* make it circular */
  border-radius: 50%;
  /* add a border around it */
  /* border: 5px solid black; */
  /* center the image */
  display: flex;
  justify-content: center;
}
/* Styles for the Navigation Bar */

.navbar {
  /* hide the navbar by default */
  display: none;
  /* stack the items vertically */
  flex-direction: row;
  /* center the text in the navbar */
  align-items: center;
  /* add some padding around the text */
  padding: 20px;
  /* add a border around the navbar */
  border: 5px solid black;


}

  </style>
</head>
<body>
  <nav>
    <h1><a href="/">TechSolutions</a></h1>
    <ul>
      <% if (user) { %>
        <li>Welcome, <%= user.firstname %></li>
        <li><a href='/'>Home</a></li>
        
        
        <!-- Profile Icon -->


      <% } else { %>
        <li><a href='/'>Home</a></li>
        <li><a href="/login">Log in</a></li>
        <li><a href="/signup" class="btn">Sign up</a></li>
      <% } %>
    </ul>
  </nav>
  
  <script>
    function toggleNavbar() {
  var navbar = document.getElementById("navbar");
  if (navbar.style.display === "none") {
    navbar.style.display = "flex";
  } else {
    navbar.style.display = "none";
  }
}

  </script>

<style>
#reschedule {

  text-align: center;
  font-size: 50px;
  color: #3096f7;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  font-weight: bold;

}
    </style>
<h1 id="reschedule">Reschedule Appointment</h1>

<form action="/appointments/<%= appointment._id %>/reschedule" method="POST">
  <div>
    <label for="date">New Date:</label>
    <input type="date" id="date" name="date" required>
  </div>
  <div>
    <label for="time">New Time:</label>
    <input type="time" id="time" name="time" required>
  </div>
  <button type="submit">Reschedule</button>
  <button type="button" onclick="window.location.href='/appointments'">Cancel</button>
</form>





<%- include('partials/footer') %>
